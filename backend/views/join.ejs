<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Join <%= event.title %></title>
  </head>
  <body>
    <h1>Join <%= event.title %></h1>
    <p>Date: <%= event.date %></p>
    <p>Participants:</p>
    <ul>
      <% event.participants.forEach(participant => { %>
      <li><%= participant.name %></li>
      <% }) %>
    </ul>
    <a href="/events/live/<%= event._id %>">Join Live Session</a>
  </body>
</html>
